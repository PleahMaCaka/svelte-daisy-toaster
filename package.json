{
	"name": "toaster-workspace",
	"private": true,
	"type": "module",
	"version": "0.1.0",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"start": "bun run build && bun run dev --workspace=examnple",
		"build": "bun run package --workspace=svelte-daisy-toaster",
		"docs:build": "npm run build --workspace=example"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
